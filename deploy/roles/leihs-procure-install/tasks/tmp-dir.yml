- file:
    path: '{{leihs_procure_dir}}/tmp'
    state: directory
    owner: '{{leihs_procure_user}}'
    recurse: yes
  name: create empty leihs-procure tmp dir

- file:
    path: '{{leihs_procure_dir}}'
    state: directory
    owner: '{{leihs_procure_user}}'
    recurse: yes
  name: set permissions for leihs-procure dir
